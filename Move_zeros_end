// Online C++ compiler to run C++ program online
#include <iostream>
#include<bits/stdc++.h>
using namespace std;

int main() {
    // Write C++ code here
    vector<int>ans={1,56,7,3,0,4,2,0,4,2,0,7,6,0,0,0,2,1,3,4,5};
    vector<int>remove;
    int n=ans.size();
    int count=0;
   
    for(int i=0;i<n;i++){
        //store non-zero element 
        if(ans[i]!=0){
            remove.push_back(ans[i]);
        }
            //count the zero element
        else{
                count++;            
        }
    }
    //k is the total size of array after findng the zeros and removing zeros
    int k=remove.size()+count;
    //now add zeros till k size
    for(int i=remove.size(); i<k;i++){
        remove.push_back(0);
    }
    //travers and print the array which zeros are moved to end
    for(int i=0;i<remove.size();i++){
        cout<<remove[i];
    }
    

    return 0;
}
